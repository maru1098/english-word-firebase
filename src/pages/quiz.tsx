import { Layout } from "src/components/layout";
import { Title } from "src/components/Title";

const Quiz = () => {
  return (
    <Layout>
      <Title>問題出題</Title>
    </Layout>
  );
};

export default Quiz;
